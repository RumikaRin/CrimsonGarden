# H5S Feature Specs

Mỗi feature Standard/Full nên có một folder riêng:

```text
H5S/specs/<feature-id>/
```

Copy `H5S/specs/_template/` sang feature mới, sau đó điền các file cần thiết.
Xem `H5S/docs/SPEC_WORKFLOW.md` để biết contract đầy đủ.

Quick mode không bắt buộc tạo spec folder.
